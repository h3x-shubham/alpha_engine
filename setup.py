from setuptools import setup, find_packages

setup(
    name="alpha_engine",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
    description="Quant-grade ML system for cross-sectional stock return prediction",
)
